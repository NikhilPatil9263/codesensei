version: "3.9"

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - chroma_data:/app/chroma_db
      - ./frontend:/app/../frontend
    env_file:
      - ./backend/.env
    restart: unless-stopped

volumes:
  chroma_data:
